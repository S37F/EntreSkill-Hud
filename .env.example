# PostgreSQL (Docker: see docker-compose.yml)
DATABASE_URL="postgresql://entreskill:entreskill@localhost:5432/entreskill?schema=public"

# NextAuth / Auth.js — use a long random string in production
AUTH_SECRET="replace-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
